version: '3.8'

services:
  backend:
    image: licitia-backend:latest
    ports:
      - "8000:8000"
    deploy:
      restart_policy:
        condition: on-failure
    networks:
      - app-network

  frontend:
    image: licitia-frontend:latest
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=https://licitia.allanturing.com/api
    deploy:
      restart_policy:
        condition: on-failure
    depends_on:
      - backend
    networks:
      - app-network

networks:
  app-network:
    driver: overlay
